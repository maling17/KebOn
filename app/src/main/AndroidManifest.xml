<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kebon">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_kres"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_kres"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditProfileActivity"></activity>
        <activity android:name=".detail.DetailActivityActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".detail.DetailTanamanActivity" />
        <activity android:name=".detail.DetailBenihActivity" />
        <activity android:name=".detail.DetailAlatActivity" />
        <activity android:name=".detail.DetailPackageActivity"></activity>
        <activity android:name=".TipsActivity" />
        <activity android:name=".ProdukPopulerActivity" />
        <activity android:name=".StarterPackActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>